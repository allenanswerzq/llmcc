===============================================================================
enum-as-field-type
===============================================================================
Test enums used as field types.
lang: python

--- file: sample/__init__.py ---
# Sample package

--- file: sample/enums.py ---
from enum import Enum, auto

class Priority(Enum):
    LOW = auto()
    MEDIUM = auto()
    HIGH = auto()
    CRITICAL = auto()

class Status(Enum):
    DRAFT = "draft"
    PENDING = "pending"
    ACTIVE = "active"
    ARCHIVED = "archived"

lang: python

--- file: sample/task.py ---
from dataclasses import dataclass
from .enums import Priority, Status

@dataclass
class Task:
    id: int
    title: str
    priority: Priority
    status: Status

@dataclass
class Project:
    name: str
    default_priority: Priority

--- expect:block-graph ---
(root:1 __init__ ($TMP/sample/__init__.py))

(root:2 enums ($TMP/sample/enums.py)
  (class:3 Priority
  )
  (class:8 Status)
)

(root:9 task ($TMP/sample/task.py)
  (class:10 Task)
  (class:11 Project)
)
