===============================================================================
enum
===============================================================================
Test that enums appear correctly in the graph.
lang: python

--- file: sample/__init__.py ---
# Sample package

--- file: sample/status.py ---
from enum import Enum, auto

class Color(Enum):
    RED = 1
    GREEN = 2
    BLUE = 3

class Status(Enum):
    PENDING = auto()
    ACTIVE = auto()
    COMPLETED = auto()

class Priority(Enum):
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"

--- expect:block-graph ---
(root:1 __init__ ($TMP/sample/__init__.py))

(root:2 status ($TMP/sample/status.py)
  (class:3 Color)
  (class:4 Status
  )
  (class:8 Priority)
)
