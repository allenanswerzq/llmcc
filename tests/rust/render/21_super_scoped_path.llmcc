===============================================================================
super-scoped-path
===============================================================================
Uses super:: prefix for parent module paths.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
pub struct Base {
    pub id: u32,
}

mod inner {
    pub struct Child {
        pub base: super::Base,
    }
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (class:2 Base
    (field:3 id @type u32)
  )
  (module:4 inner (inline)
    (class:5 Child
      (field:6 base @type:2 Base)
      (@tdep:2 Base)
    )
  )
)

--- expect:arch-graph ---
digraph architecture {
  // Layout settings
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    style=rounded;
    color="#888888";
    bgcolor="#f0f0f0";

    subgraph cluster_lib_rs {
      label="lib.rs";
      style=rounded;
      color="#aaaaaa";
      bgcolor="#fafafa";

      n2[label="Base", full_path="$TMP/sample/src/lib.rs:1", sym_ty="Struct", shape=box];
      n5[label="Child", full_path="$TMP/sample/src/lib.rs:6", sym_ty="Struct", shape=box];
    }

  }


  n2 -> n5 [from="field_type", to="struct"];
}
