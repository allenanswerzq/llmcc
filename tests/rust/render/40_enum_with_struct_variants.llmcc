===============================================================================
enum-with-struct-variants
===============================================================================
Test enums with struct-like variants containing user-defined types.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod types;
mod events;

--- file: sample/src/types.rs ---
pub struct User {
    pub id: u64,
    pub name: String,
}

pub struct Order {
    pub id: u64,
    pub amount: f64,
}

pub struct Error {
    pub code: i32,
    pub message: String,
}

--- file: sample/src/events.rs ---
use crate::types::{User, Order, Error};

pub enum Event {
    UserCreated {
        user: User,
        timestamp: u64,
    },
    OrderPlaced {
        order: Order,
        user: User,
    },
    Failed {
        error: Error,
    },
    Empty,
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 types)
  (module:3 events)
)

(root:4 types ($TMP/sample/src/types.rs)
  (class:5 User
    (field:6 id @type u64)
    (field:7 name @type String)
  )
  (class:8 Order
    (field:9 id @type u64)
    (field:10 amount @type f64)
  )
  (class:11 Error
    (field:12 code @type i32)
    (field:13 message @type String)
  )
)

(root:14 events ($TMP/sample/src/events.rs)
  (enum:15 Event
    (field:16 UserCreated
      (field:17 user @type:5 User)
      (field:18 timestamp @type u64)
    )
    (field:19 OrderPlaced
      (field:20 order @type:8 Order)
      (field:21 user @type:5 User)
    )
    (field:22 Failed
      (field:23 error @type:11 Error)
    )
    (field:24 Empty)
  )
)

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    bgcolor="#f8f8f8";

    subgraph cluster_events_rs {
      label="events.rs";
      bgcolor="#fafafa";

      n15[label="Event", full_path="$TMP/sample/src/events.rs:3", sym_ty="Enum", shape=box];
    }

    subgraph cluster_types_rs {
      label="types.rs";
      bgcolor="#fafafa";

      n5[label="User", full_path="$TMP/sample/src/types.rs:1", sym_ty="Struct", shape=box];
      n11[label="Error", full_path="$TMP/sample/src/types.rs:11", sym_ty="Struct", shape=box];
      n8[label="Order", full_path="$TMP/sample/src/types.rs:6", sym_ty="Struct", shape=box];
    }

  }


  n5 -> n15 [from="field_type", to="enum"];
  n8 -> n15 [from="field_type", to="enum"];
  n11 -> n15 [from="field_type", to="enum"];
}
