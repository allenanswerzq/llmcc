===============================================================================
crate-scoped-path
===============================================================================
Uses crate:: prefix for type resolution.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod models;
mod services;

--- file: sample/src/models.rs ---
pub struct Config {
    pub debug: bool,
}

--- file: sample/src/services.rs ---
pub struct App {
    pub config: crate::models::Config,
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs))

(root:2 models ($TMP/sample/src/models.rs)
  (class:3 Config
    (field:4 debug @type bool)
  )
)

(root:5 services ($TMP/sample/src/services.rs)
  (class:6 App
    (field:7 config @type:3 Config)
  )
)

--- expect:arch-graph ---
digraph architecture {
  // Layout settings
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  label="architecture graph";
  labelloc=t;

  subgraph cluster_sample {
    label="sample";
    style=rounded;
    color="#888888";
    bgcolor="#f0f0f0";

    subgraph cluster_models_rs {
      label="models.rs";
      style=rounded;
      color="#aaaaaa";
      bgcolor="#fafafa";

      n3[label="Config", full_path="$TMP/sample/src/models.rs:1", sym_ty="Struct", shape=box];
    }

    subgraph cluster_services_rs {
      label="services.rs";
      style=rounded;
      color="#aaaaaa";
      bgcolor="#fafafa";

      n6[label="App", full_path="$TMP/sample/src/services.rs:1", sym_ty="Struct", shape=box];
    }

  }


  n3 -> n6 [from="field_type", to="struct"];
}
