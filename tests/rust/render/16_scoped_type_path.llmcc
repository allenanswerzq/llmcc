===============================================================================
scoped-type-path
===============================================================================
Field uses fully scoped path type (module::Type).

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod types;

pub struct Container {
    pub item: types::Item,
}

--- file: sample/src/types.rs ---
pub struct Item {
    pub id: u32,
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 types)
  (class:3 Container
    (field:4 item @type:6 Item)
    (@tdep:6 Item)
  )
)

(root:5 types ($TMP/sample/src/types.rs)
  (class:6 Item
    (field:7 id @type u32)
  )
)

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    bgcolor="#f8f8f8";

    subgraph cluster_lib_rs {
      label="lib.rs";
      bgcolor="#fafafa";

      n3[label="Container", full_path="$TMP/sample/src/lib.rs:3", sym_ty="Struct", shape=box];
    }

    subgraph cluster_types_rs {
      label="types.rs";
      bgcolor="#fafafa";

      n6[label="Item", full_path="$TMP/sample/src/types.rs:1", sym_ty="Struct", shape=box];
    }

  }


  n6 -> n3 [from="field_type", to="struct"];
}
