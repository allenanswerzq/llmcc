===============================================================================
cross-file-trait-impl
===============================================================================
Struct in one file implements trait from another file.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod traits;
mod impls;

--- file: sample/src/traits.rs ---
pub trait Render {
    fn render(&self) -> String;
}

--- file: sample/src/impls.rs ---
use crate::traits::Render;

pub struct Html {
    pub content: String,
}

impl Render for Html {
    fn render(&self) -> String {
        format!("<p>{}</p>", self.content)
    }
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 traits)
  (module:3 impls)
)

(root:4 traits ($TMP/sample/src/traits.rs)
  (trait:5 Render
    (method:6 render
      (parameter:7 self @type:5 Render)
      (return:8 @type String)
    )
  )
)

(root:9 impls ($TMP/sample/src/impls.rs)
  (class:10 Html
    (field:11 content @type String)
  )
  (impl:12 Html @type:10 @trait:5
    (method:13 render
      (parameter:14 self @type:10 Html)
      (return:15 @type String)
    )
  )
)

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    bgcolor="#f8f8f8";

    subgraph cluster_impls_rs {
      label="impls.rs";
      bgcolor="#fafafa";

      n10[label="Html", full_path="$TMP/sample/src/impls.rs:3", sym_ty="Struct", shape=box];
    }

    subgraph cluster_traits_rs {
      label="traits.rs";
      bgcolor="#fafafa";

      n5[label="Render", full_path="$TMP/sample/src/traits.rs:1", sym_ty="Trait", shape=box];
    }

  }


  n5 -> n10 [from="trait", to="impl"];
}
