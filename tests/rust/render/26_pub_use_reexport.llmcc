===============================================================================
pub-use-reexport
===============================================================================
Re-exported type via pub use.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod internal;
pub use internal::Secret;

mod consumer;

--- file: sample/src/internal.rs ---
pub struct Secret {
    pub key: String,
}

--- file: sample/src/consumer.rs ---
use crate::Secret;

pub struct Consumer {
    pub secret: Secret,
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 internal)
  (module:3 consumer)
)

(root:4 internal ($TMP/sample/src/internal.rs)
  (class:5 Secret
    (field:6 key @type String)
  )
)

(root:7 consumer ($TMP/sample/src/consumer.rs)
  (class:8 Consumer
    (field:9 secret @type:5 Secret)
    (@tdep:5 Secret)
  )
)

--- expect:arch-graph ---
digraph architecture {
  // Layout settings
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    style=rounded;
    color="#888888";
    bgcolor="#f0f0f0";

    subgraph cluster_consumer_rs {
      label="consumer.rs";
      style=rounded;
      color="#aaaaaa";
      bgcolor="#fafafa";

      n8[label="Consumer", full_path="$TMP/sample/src/consumer.rs:3", sym_ty="Struct", shape=box];
    }

    subgraph cluster_internal_rs {
      label="internal.rs";
      style=rounded;
      color="#aaaaaa";
      bgcolor="#fafafa";

      n5[label="Secret", full_path="$TMP/sample/src/internal.rs:1", sym_ty="Struct", shape=box];
    }

  }


  n5 -> n8 [from="field_type", to="struct"];
  n5 -> n8 [from="interface", to="implements"];
}
