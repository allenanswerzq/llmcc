===============================================================================
cross-file-func-param
===============================================================================
Function in one file takes struct from another file as parameter.

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod types;
mod handler;

--- file: sample/src/types.rs ---
pub struct Request {
    pub body: String,
}

--- file: sample/src/handler.rs ---
use crate::types::Request;

pub fn handle(req: Request) {
    println!("{}", req.body);
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 types)
  (module:3 handler)
)

(root:4 types ($TMP/sample/src/types.rs)
  (class:5 Request
    (field:6 body @type String)
  )
)

(root:7 handler ($TMP/sample/src/handler.rs)
  (func:8 handle
    (parameter:9 req @type:5 Request)
  )
)

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    style=rounded;
        bgcolor="#f8f8f8";

    subgraph cluster_handler_rs {
      label="handler.rs";
      style=rounded;
            bgcolor="#fafafa";

      n8[label="handle", path="$TMP/sample/src/handler.rs:3", sym_ty="Function", shape=ellipse];
    }

    subgraph cluster_types_rs {
      label="types.rs";
      style=rounded;
            bgcolor="#fafafa";

      n5[label="Request", path="$TMP/sample/src/types.rs:1", sym_ty="Struct", shape=box];
    }

  }


  n5 -> n8 [from="input", to="func"];
}
