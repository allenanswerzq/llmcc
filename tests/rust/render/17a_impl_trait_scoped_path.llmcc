===============================================================================
impl-trait-scoped-path
===============================================================================
Struct implements trait using scoped path (crate::module::Trait).

--- file: sample/Cargo.toml ---
[package]
name = "sample"

--- file: sample/src/lib.rs ---
mod animal;
mod dog;

--- file: sample/src/animal.rs ---
pub trait Animal {
    fn speak(&self);
}

--- file: sample/src/dog.rs ---
pub struct Dog {
    pub name: String,
}

impl crate::animal::Animal for Dog {
    fn speak(&self) {
        println!("Woof!");
    }
}

--- expect:block-graph ---
(root:1 lib ($TMP/sample/src/lib.rs)
  (module:2 animal)
  (module:3 dog)
)

(root:4 animal ($TMP/sample/src/animal.rs)
  (trait:5 Animal
    (method:6 speak
      (parameter:7 self @type:5 Animal)
    )
  )
)

(root:8 dog ($TMP/sample/src/dog.rs)
  (class:9 Dog
    (field:10 name @type String)
  )
  (impl:11 Dog @type:9 @trait:5
    (method:12 speak
      (parameter:13 self @type:9 Dog)
    )
  )
)

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_sample {
    label="sample";
    bgcolor="#f8f8f8";

    subgraph cluster_animal_rs {
      label="animal.rs";
      bgcolor="#fafafa";

      n5[label="Animal", full_path="$TMP/sample/src/animal.rs:1", sym_ty="Trait", shape=box];
    }

    subgraph cluster_dog_rs {
      label="dog.rs";
      bgcolor="#fafafa";

      n9[label="Dog", full_path="$TMP/sample/src/dog.rs:1", sym_ty="Struct", shape=box];
    }

  }


  n5 -> n9 [from="trait", to="impl"];
}
