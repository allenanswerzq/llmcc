===============================================================================
template-class
===============================================================================
Test that template classes appear in the architecture graph.
lang: cpp

--- file: sample/CMakeLists.txt ---
cmake_minimum_required(VERSION 3.10)
project(sample)

--- file: sample/src/lib.cpp ---

template<typename T>
class Container {
public:
    T value;

    T get() const {
        return value;
    }

    void set(T v) {
        value = v;
    }
};

template<typename K, typename V>
class Pair {
public:
    K key;
    V val;
};

--- expect:block-graph ---
(root:1 template<typename T>
class Container {
public:
    T value;

    T get() const {
        return value;
    }

    void set(T v) {
        value = v;
    }
};

template<typename K, typename V>
class Pair {
public:
    K key;
    V val;
};
 ($TMP/sample/src/lib.cpp)
  (func:2 template<typename T>
class Container {
public:
    T value;

    T get() const {
        return value;
    }

    void set(T v) {
        value = v;
    }
};
    (class:3 Container
      (field:4 T @type value)
      (method:5 get
        (func:6 get)
      )
      (method:7 set
        (func:8 set
          (parameter:9 v @type v)
        )
      )
    )
    (@tdep:3 Container)
  )
  (func:10 template<typename K, typename V>
class Pair {
public:
    K key;
    V val;
};
    (class:11 Pair
      (field:12 K @type key)
      (field:13 V @type val)
    )
    (@tdep:11 Pair)
  )
)
