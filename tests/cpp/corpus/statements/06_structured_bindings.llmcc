===============================================================================
structured-bindings
===============================================================================
Structured binding declarations (C++17).
lang: cpp

--- file: src/lib.cpp ---
struct Point { int x, y; };

void test_bindings() {
    Point p{10, 20};
    auto [x, y] = p;

    int arr[3] = {1, 2, 3};
    auto [a, b, c] = arr;

    auto& [rx, ry] = p;
    rx = 30;
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (class:2 Point
    (field:3 int @type int)
  )
  (func:4 test_bindings)
)
