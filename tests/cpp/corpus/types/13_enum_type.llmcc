===============================================================================
enum-type
===============================================================================
Enum types.
lang: cpp

--- file: src/lib.cpp ---
enum Color { Red, Green, Blue };

enum class Status {
    Ok,
    Error,
    Pending
};

void use_enum() {
    Color c = Red;
    Status s = Status::Ok;

    int i = c;
    int j = static_cast<int>(s);
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (enum:2 Color
    (field:3 Red @type:3 Red)
    (field:4 Green @type:4 Green)
    (field:5 Blue @type:5 Blue)
  )
  (enum:6 Status
    (field:7 Ok @type:7 Ok)
    (field:8 Error @type:8 Error)
    (field:9 Pending @type:9 Pending)
  )
  (func:10 use_enum
  )
)
