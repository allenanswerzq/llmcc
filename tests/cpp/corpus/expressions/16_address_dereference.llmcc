===============================================================================
address-dereference
===============================================================================
Address-of and dereference expressions.
lang: cpp

--- file: src/lib.cpp ---
void test_pointers() {
    int x = 42;
    int* ptr = &x;
    int y = *ptr;

    int** pptr = &ptr;
    int z = **pptr;

    int arr[3] = {1, 2, 3};
    int* p = &arr[0];
    int* q = &*p;
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (func:2 test_pointers)
)
