===============================================================================
comma-operator
===============================================================================
Comma operator expressions.
lang: cpp

--- file: src/lib.cpp ---
void test_comma() {
    int a, b, c;
    a = (b = 1, c = 2, b + c);

    for (int i = 0, j = 10; i < j; i++, j--) {
    }
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (func:2 test_comma)
)
