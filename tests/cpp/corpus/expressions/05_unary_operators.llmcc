===============================================================================
unary-operators
===============================================================================
Unary operator expressions.
lang: cpp

--- file: src/lib.cpp ---
void test_unary() {
    int a = 5;
    int b = -a;
    int c = +a;
    bool d = !true;
    int e = ~a;
    int f = ++a;
    int g = a--;
}

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (func:2 test_unary)
)
