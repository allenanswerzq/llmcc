===============================================================================
binary-operators
===============================================================================
Binary operator expressions.
lang: cpp

--- file: src/lib.cpp ---
void test_binary() {
    int a = 5 + 3;
    int b = 10 - 2;
    int c = 4 * 3;
    int d = 12 / 4;
    int e = 7 % 3;

    bool f = 5 > 3;
    bool g = 2 < 8;
    bool h = 3 >= 3;
    bool i = 4 <= 5;
    bool j = 1 == 1;
    bool k = 2 != 3;

    bool l = true && false;
    bool m = true || false;

    int n = 5 & 3;
    int o = 5 | 3;
    int p = 5 ^ 3;
    int q = 5 << 2;
    int r = 20 >> 2;
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (func:2 test_binary)
)
