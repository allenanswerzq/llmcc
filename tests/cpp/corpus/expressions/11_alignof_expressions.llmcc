===============================================================================
alignof-expressions
===============================================================================
Alignof expressions.
lang: cpp

--- file: src/lib.cpp ---
struct Point { int x, y; };

void test_alignof() {
    int a = alignof(int);
    int b = alignof(double);
    int c = alignof(Point);
    int d = alignof(void*);
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (class:2 Point
    (field:3 int @type int)
  )
  (func:4 test_alignof)
)
