===============================================================================
sizeof-expressions
===============================================================================
Sizeof expressions.
lang: cpp

--- file: src/lib.cpp ---
struct Point { int x, y; };

void test_sizeof() {
    int a = sizeof(int);
    int b = sizeof(Point);

    Point p;
    int c = sizeof(p);
    int d = sizeof p;

    int arr[10];
    int e = sizeof(arr);
    int f = sizeof(arr[0]);
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (class:2 Point
    (field:3 int @type int)
  )
  (func:4 test_sizeof)
)
