===============================================================================
variadic-functions
===============================================================================
Variadic C-style functions.
lang: cpp

--- file: src/lib.cpp ---
void printf_like(const char* format, ...) {}

int sum(int count, ...) {
    return 0;
}

class Logger {
public:
    void log(const char* format, ...) {}
};

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.cpp)
  (func:2 printf_like
    (parameter:3 format @type char)
  )
  (func:4 sum
    (parameter:5 count @type int)
  )
  (class:6 Logger
    (method:7 log
      (parameter:8 format @type char)
    )
  )
)
