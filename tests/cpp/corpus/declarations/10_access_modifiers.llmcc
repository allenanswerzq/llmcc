===============================================================================
access-modifiers
===============================================================================
Access modifiers in classes.
lang: cpp

--- file: src/lib.cpp ---
class Account {
public:
    std::string owner;
    double balance;

protected:
    int account_id;

private:
    std::string password;

public:
    void deposit(double amount);
    void withdraw(double amount);

private:
    bool validate_password(std::string pw);
};

--- expect:block-graph ---
(root:1 class Account {
public:
    std::string owner;
    double balance;

protected:
    int account_id;

private:
    std::string password;

public:
    void deposit(double amount);
    void withdraw(double amount);

private:
    bool validate_password(std::string pw);
};
 ($TMP/src/lib.cpp)
  (class:2 Account
    (field:3 std @type owner)
    (field:4 double @type double)
    (field:5 int @type int)
    (field:6 std @type password)
    (field:7 void @type void)
    (field:8 void @type void)
    (field:9 bool @type bool)
  )
)
