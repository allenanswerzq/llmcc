===============================================================================
destructuring
===============================================================================
Destructuring declarations.
lang: typescript

--- file: src/lib.ts ---
const [a, b] = [1, 2];
const { x, y } = { x: 1, y: 2 };
const { name: alias } = { name: "test" };

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts))
