===============================================================================
multiple-extends
===============================================================================
Multiple extends in interface.
lang: typescript

--- file: src/lib.ts ---
interface Named {
    name: string;
}

interface Aged {
    age: number;
}

interface Located {
    location: string;
}

interface Person extends Named, Aged, Located {
    id: number;
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts)
  (interface:2 Named
    (field:3 name @type string)
  )
  (interface:4 Aged
    (field:5 age @type number)
  )
  (interface:6 Located
    (field:7 location @type string)
  )
  (interface:8 Person @extends:2 Named @extends:4 Aged @extends:6 Located
    (field:9 id @type number)
  )
)
