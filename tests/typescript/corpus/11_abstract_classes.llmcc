===============================================================================
abstract-classes
===============================================================================
Abstract classes and methods.
lang: typescript

--- file: src/lib.ts ---
abstract class Shape {
    abstract area(): number;
    abstract perimeter(): number;

    describe(): string {
        return `Area: ${this.area()}`;
    }
}

abstract class Animal {
    abstract name: string;
    abstract makeSound(): void;

    move(): void {
        console.log("Moving...");
    }
}

class Circle extends Shape {
    radius: number;

    area(): number {
        return Math.PI * this.radius ** 2;
    }

    perimeter(): number {
        return 2 * Math.PI * this.radius;
    }
}

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (class:2 Shape
    (method:3 area
      (return:4 @type number)
    )
    (method:5 perimeter
      (return:6 @type number)
    )
    (method:7 describe
      (return:8 @type string)
    )
  )
  (class:10 Animal
    (field:11 name @type string)
    (method:12 makeSound
      (return:13 @type void)
    )
    (method:14 move
      (return:15 @type void)
    )
  )
  (class:17 Circle @extends:2 Shape
    (field:18 radius @type number)
    (method:19 area
      (return:20 @type number)
    )
    (method:21 perimeter
      (return:22 @type number)
    )
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=3 edges=1 clusters=1
//
digraph architecture {

  subgraph cluster_root_0_000_lib_ts {
    label="000_lib.ts";

    n2[label="Shape", file="000_lib.ts", path="$TMP/src/000_lib.ts:1", kind="struct"];
    n10[label="Animal", file="000_lib.ts", path="$TMP/src/000_lib.ts:10", kind="struct"];
    n17[label="Circle", file="000_lib.ts", path="$TMP/src/000_lib.ts:19", kind="struct"];
  }


n2 -> n17 [rel="specialization", from="base", to="specializes"];
}
