===============================================================================
template-literals
===============================================================================
Template string literals with interpolation.
lang: typescript

--- file: src/lib.ts ---
const name = "world";
const greeting = `Hello, ${name}!`;
const complex = `Sum: ${1 + 2}`;

--- expect:block-graph ---
(root:1 000_lib (000_lib))
