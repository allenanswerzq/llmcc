===============================================================================
regex-literals
===============================================================================
Regular expression literals.
lang: typescript

--- file: src/lib.ts ---
const simple = /abc/;
const withFlags = /pattern/gi;
const escaped = /\d+/;
const complex = /^[a-z]+$/im;

--- expect:block-graph ---
(root:1 000_lib (000_lib))
