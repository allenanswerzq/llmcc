===============================================================================
tagged-templates
===============================================================================
Tagged template expressions.
lang: typescript

--- file: src/lib.ts ---
function tag(strings: TemplateStringsArray, ...values: unknown[]): string {
    return strings.join("");
}

const name = "world";
const result = tag`Hello, ${name}!`;

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (func:2 tag
    (parameter:3 strings @type strings)
    (parameter:4 values @type unknown)
    (return:5 @type string)
  )
)
