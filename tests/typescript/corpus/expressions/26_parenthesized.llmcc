===============================================================================
parenthesized-expression
===============================================================================
Parenthesized expressions.
lang: typescript

--- file: src/lib.ts ---
const a = (1 + 2) * 3;
const b = ((1 + 2) * (3 + 4));
const c = (true ? 1 : 2) + 3;

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts))
