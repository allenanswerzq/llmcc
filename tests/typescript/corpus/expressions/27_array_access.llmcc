===============================================================================
array-element-access
===============================================================================
Array element access expressions.
lang: typescript

--- file: src/lib.ts ---
const arr = [1, 2, 3];
const first = arr[0];
const last = arr[arr.length - 1];
const nested = [[1, 2], [3, 4]];
const element = nested[0][1];

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts))
