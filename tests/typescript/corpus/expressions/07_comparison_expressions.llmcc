===============================================================================
comparison-expressions
===============================================================================
Comparison and equality expressions.
lang: typescript

--- file: src/lib.ts ---
const eq = 1 === 2;
const neq = 1 !== 2;
const lt = 1 < 2;
const gt = 1 > 2;
const lte = 1 <= 2;
const gte = 1 >= 2;

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts))
