===============================================================================
ternary-expressions
===============================================================================
Conditional ternary expressions.
lang: typescript

--- file: src/lib.ts ---
const result = true ? "yes" : "no";
const nested = true ? (false ? 1 : 2) : 3;

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts))
