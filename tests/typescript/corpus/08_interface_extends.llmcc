===============================================================================
interface-extends
===============================================================================
Interfaces extending other interfaces.
lang: typescript

--- file: src/lib.ts ---
interface Animal {
    name: string;
}

interface Mammal extends Animal {
    warmBlooded: boolean;
}

interface Dog extends Mammal {
    breed: string;
    bark(): void;
}

interface Named {
    name: string;
}

interface Aged {
    age: number;
}

interface Person extends Named, Aged {
    email: string;
}

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (interface:2 Animal
    (field:3 name @type string)
  )
  (interface:4 Mammal @extends:2 Animal
    (field:5 warmBlooded @type boolean)
  )
  (interface:6 Dog @extends:4 Mammal
    (field:7 breed @type string)
    (method:8 bark
      (return:9 @type void)
    )
  )
  (interface:10 Named
    (field:11 name @type string)
  )
  (interface:12 Aged
    (field:13 age @type number)
  )
  (interface:14 Person @extends:10 Named @extends:12 Aged
    (field:15 email @type string)
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=6 edges=4 clusters=1
//
digraph architecture {

  subgraph cluster_root_0_000_lib_ts {
    label="000_lib.ts";

    n2[label="Animal", file="000_lib.ts", path="$TMP/src/000_lib.ts:1", kind="interface"];
    n4[label="Mammal", file="000_lib.ts", path="$TMP/src/000_lib.ts:5", kind="interface"];
    n6[label="Dog", file="000_lib.ts", path="$TMP/src/000_lib.ts:9", kind="interface"];
    n10[label="Named", file="000_lib.ts", path="$TMP/src/000_lib.ts:14", kind="interface"];
    n12[label="Aged", file="000_lib.ts", path="$TMP/src/000_lib.ts:18", kind="interface"];
    n14[label="Person", file="000_lib.ts", path="$TMP/src/000_lib.ts:22", kind="interface"];
  }


n2 -> n4 [rel="specialization", from="base", to="specializes"];
n4 -> n6 [rel="specialization", from="base", to="specializes"];
n10 -> n14 [rel="specialization", from="base", to="specializes"];
n12 -> n14 [rel="specialization", from="base", to="specializes"];
}
