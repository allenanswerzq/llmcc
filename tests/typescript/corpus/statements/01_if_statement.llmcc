===============================================================================
if-statement
===============================================================================
If statement with else branches.
lang: typescript

--- file: src/lib.ts ---
function check(x: number): string {
    if (x > 0) {
        return "positive";
    } else if (x < 0) {
        return "negative";
    } else {
        return "zero";
    }
}

--- expect:block-graph ---
(root:1 lib ($TMP/src/lib.ts)
  (func:2 check
    (parameter:3 x @type number)
    (return:4 @type string)
  )
)
