===============================================================================
cross-file-func-return
===============================================================================
Test that cross-file function return types create edges.
lang: typescript

--- file: sample/src/response.ts ---

export class Response {
    status: number;
    body: string;
}

--- file: sample/src/api.ts ---
import { Response } from './response';

export function fetchData(): Response {
    return new Response();
}

--- expect:arch-graph ---
digraph architecture {
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_api_ts {
    label="api.ts";
    bgcolor="#fafafa";

    n6[label="fetchData", full_path="$TMP/sample/src/api.ts:3", sym_ty="Function", shape=ellipse];
  }

  subgraph cluster_response_ts {
    label="response.ts";
    bgcolor="#fafafa";

    n2[label="Response", full_path="$TMP/sample/src/response.ts:2", sym_ty="Struct", shape=box];
  }


  n6 -> n2 [from="func", to="output"];
}
