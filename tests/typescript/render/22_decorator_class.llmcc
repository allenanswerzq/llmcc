===============================================================================
decorator-class
===============================================================================
Test that decorators on classes create edges.
lang: typescript

--- file: sample/src/lib.ts ---

function Component(name: string) {
    return function(target: any) {
        target.componentName = name;
    };
}

function Injectable() {
    return function(target: any) {};
}

@Component("app")
@Injectable()
class AppComponent {
    title: string;
}

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (func:2 Component
    (parameter:3 name @type string)
    (parameter:4 target @type any)
  )
  (func:5 Injectable
    (parameter:6 target @type any)
  )
  (class:7 AppComponent
    (field:10 title @type string)
    (@tdep:2 Component)
    (@tdep:5 Injectable)
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=3 edges=2 clusters=1
//
digraph architecture {

  subgraph cluster_root_0_000_lib_ts {
    label="000_lib.ts";

    n2[label="Component", file="000_lib.ts", path="$TMP/sample/src/000_lib.ts:2", kind="function"];
    n5[label="Injectable", file="000_lib.ts", path="$TMP/sample/src/000_lib.ts:8", kind="function"];
    n7[label="AppComponent", file="000_lib.ts", path="$TMP/sample/src/000_lib.ts:12", kind="struct"];
  }


n2 -> n7 [rel="annotation", from="annotation", to="annotates"];
n5 -> n7 [rel="annotation", from="annotation", to="annotates"];
}
