===============================================================================
cross-file-interface-impl
===============================================================================
Test that cross-file interface implementations create edges.
lang: typescript

--- file: sample/src/animal.ts ---

export interface Animal {
    speak(): void;
}

--- file: sample/src/dog.ts ---
import { Animal } from './animal';

export class Dog implements Animal {
    name: string;

    speak(): void {
        console.log("Woof!");
    }
}

--- expect:arch-graph ---
digraph architecture {
  // Layout settings
  rankdir=TB;
  ranksep=0.6;
  nodesep=0.3;
  splines=ortho;
  compound=true;

  node [shape=box, style=rounded];
  edge [arrowsize=0.7];

  subgraph cluster_animal_ts {
    label="animal.ts";
    style=rounded;
    color="#aaaaaa";
    bgcolor="#fafafa";

    n2[label="Animal", full_path="$TMP/sample/src/animal.ts:2", sym_ty="Interface", shape=box];
  }

  subgraph cluster_dog_ts {
    label="dog.ts";
    style=rounded;
    color="#aaaaaa";
    bgcolor="#fafafa";

    n6[label="Dog", full_path="$TMP/sample/src/dog.ts:3", sym_ty="Struct", shape=box];
  }


  n2 -> n6 [from="interface", to="implements"];
}
