===============================================================================
cross-file-reference
===============================================================================
Test that types defined in different files create edges.
lang: typescript

--- file: sample/src/types.ts ---

export class User {
    id: number;
    name: string;
}

--- file: sample/src/services.ts ---
import { User } from './types';

export class UserService {
    currentUser: User;
}

export function getUser(): User {
    return new User();
}

--- expect:block-graph ---
(root:1 000_types (000_types)
  (class:2 User
    (field:3 id @type number)
    (field:4 name @type string)
  )
)

(root:5 001_services (001_services)
  (class:6 UserService
    (field:7 currentUser @type:2 User)
  )
  (func:8 getUser
    (return:9 @type:2 User)
    (@tdep:2 User)
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=3 edges=2 clusters=2
//
digraph architecture {

  subgraph cluster_root_0_000_types_ts {
    label="000_types.ts";

    n2[label="User", file="000_types.ts", path="$TMP/sample/src/000_types.ts:2", kind="struct"];
  }

  subgraph cluster_root_1_001_services_ts {
    label="001_services.ts";

    n6[label="UserService", file="001_services.ts", path="$TMP/sample/src/001_services.ts:3", kind="struct"];
    n8[label="getUser", file="001_services.ts", path="$TMP/sample/src/001_services.ts:7", kind="function"];
  }


n2 -> n6 [rel="field", from="field_type", to="container"];
n8 -> n2 [rel="return", from="func", to="output"];
}
