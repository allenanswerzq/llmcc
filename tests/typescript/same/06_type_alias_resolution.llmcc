===============================================================================
type-alias-resolution
===============================================================================
Test that type aliases resolve correctly when there are same-named types.
lang: typescript

--- file: src/internal.ts ---

export interface Error {
    internalCode: number;
}

export type Result<T> = T | Error;

--- file: src/public.ts ---

interface Error {
    userMessage: string;
}

// Public Result uses public Error
type Result<T> = T | Error;

export class Api {
    lastError: Error;
}

--- expect:block-graph ---
(root:1 000_internal (000_internal)
  (interface:2 Error
    (field:3 internalCode @type number)
  )
  (alias:4 Result)
)

(root:5 001_public (001_public)
  (interface:6 Error
    (field:7 userMessage @type string)
  )
  (alias:8 Result)
  (class:9 Api
    (field:10 lastError @type:6 Error)
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=3 edges=1 clusters=2
//
digraph architecture {

  subgraph cluster_root_0_000_internal_ts {
    label="000_internal.ts";

    n2[label="Error", file="000_internal.ts", path="$TMP/src/000_internal.ts:2", kind="interface"];
  }

  subgraph cluster_root_1_001_public_ts {
    label="001_public.ts";

    n6[label="Error", file="001_public.ts", path="$TMP/src/001_public.ts:2", kind="interface"];
    n9[label="Api", file="001_public.ts", path="$TMP/src/001_public.ts:9", kind="struct"];
  }


n6 -> n9 [rel="field", from="field_type", to="container"];
}
