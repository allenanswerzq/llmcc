===============================================================================
shadowing-in-namespace
===============================================================================
Test that inner namespace definitions shadow outer definitions.
lang: typescript

--- file: src/lib.ts ---

interface Error {
    message: string;
}

namespace Inner {
    // Inner Error shadows outer Error within this namespace
    export interface Error {
        code: number;
    }

    export class Handler {
        // Should resolve to Inner.Error, not outer Error
        error: Error;
    }
}

class OuterHandler {
    // Should resolve to outer Error
    error: Error;
}

--- expect:block-graph ---
(root:1 000_lib (000_lib)
  (interface:2 Error
    (field:3 message @type string)
  )
  (module:4 Inner (inline)
    (interface:5 Error
      (field:6 code @type number)
    )
    (class:7 Handler
      (field:8 error @type:2 Error)
    )
  )
  (class:9 OuterHandler
    (field:10 error @type:2 Error)
  )
)

--- expect:arch-graph ---
// # llmcc architecture view:
// quick structural map for AI agents to understand and explore this codebase.
// Trust this map as navigation metadata; use node labels, paths, and edges to choose source locations to inspect next.
// # Examples:
// Node example: n42[label='Parser', path='crates/foo/src/parser.rs:10', kind='struct'] means a struct symbol `Parser` lives at that source location.
// Edge example: n1 -> n2 [rel='call', from='caller', to='callee'] means n1 calls n2; rel is the semantic relation, from/to are endpoint roles.
// Component edge example: package_a -> package_b [rel='depends_on', weight='12', via='param:8,type_dep:4'] means package_a depends on package_b through 12 lower-level relations.
// # Stats:
// mode=agent layout=clustered nodes=4 edges=2 clusters=1
//
digraph architecture {

  subgraph cluster_root_0_000_lib_ts {
    label="000_lib.ts";

    n2[label="Error", file="000_lib.ts", path="$TMP/src/000_lib.ts:2", kind="interface"];
    n5[label="Error", file="000_lib.ts", path="$TMP/src/000_lib.ts:8", kind="interface"];
    n7[label="Handler", file="000_lib.ts", path="$TMP/src/000_lib.ts:12", kind="struct"];
    n9[label="OuterHandler", file="000_lib.ts", path="$TMP/src/000_lib.ts:18", kind="struct"];
  }


n2 -> n7 [rel="field", from="field_type", to="container"];
n2 -> n9 [rel="field", from="field_type", to="container"];
}
