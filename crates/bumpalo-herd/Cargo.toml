[package]
name = "bumpalo-herd"
version.workspace = true
edition = "2021"
description = "Thread-safe bumpalo wrapper with pre-allocation support"
license.workspace = true
repository.workspace = true

[dependencies]
bumpalo = { version = "3.14", features = ["collections"] }
