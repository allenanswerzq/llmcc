repo = "https://github.com/tokio-rs/tokio"

[[tasks]]
id = "tokio-find-spawn-blocking"
description = """
Find the file and function where spawn_blocking is implemented.
1. The file path where spawn_blocking is defined
2. The main struct that manages the blocking thread pool
3. How tasks are dispatched to blocking threads
"""

[[tasks]]
id = "tokio-trace-select-macro"
description = """
Find how the tokio::select! macro is implemented:
1. Where is the macro defined?
2. What internal types does it generate?
3. How does the polling loop work?
"""
